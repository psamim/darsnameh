<div>
<%= @lesson.text %>
</div>


<br />
<br />
<h3 class="page-header ">Questions</h3>
<div><%= link_to "New Question", [:new, @lesson, :question] %>
<table class="table table-hover">
<tr>
  <th> Text </th>
  <th width="10%"> Actions</th>
</tr>
<% @lesson.questions.each do |q, lesson| %>
<tr>
  <td> <%= link_to q.text, [q] %></td>
  <td> <%= link_to raw("<span class='glyphicon glyphicon-remove'></span>"), [q], method: :delete  %> </td>
</tr>
<% end %>
</table>
